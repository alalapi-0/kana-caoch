<!--
  星级组件模板：内部实际渲染 Vant Rate，保持一层封装便于未来更换 UI 库。
  由于使用了 Vant 内置样式，此处仅保留必要的容器，aria-label 用于辅助功能。
-->
<view class="stars" aria-label="评分：{{displayStars}} / {{max}} 星" role="img">
  <van-rate readonly="true" value="{{displayStars}}" count="{{max}}" size="{{size}}" color="#fa5151"
    void-color="#ebedf0" allow-half="true" />
</view>
