/*
 * 首页样式说明：
 *  - 采用柔和渐变背景与圆角卡片，营造学习仪表盘的观感；
 *  - 与 Vant 组件配合时，保证内边距与阴影一致，避免割裂感。
 */
.home-page {
  padding: 48rpx 40rpx 80rpx;
  background: linear-gradient(180deg, #f8fafc 0%, #e2e8f0 100%);
  min-height: 100vh;
  box-sizing: border-box;
}

.home-hero {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  padding: 32rpx;
  border-radius: 28rpx;
  background: #ffffff;
  box-shadow: 0 16rpx 40rpx rgba(148, 163, 184, 0.22);
  margin-bottom: 32rpx;
}

.home-title {
  font-size: 48rpx;
  font-weight: 700;
  color: #0f172a;
}

.home-subtitle {
  font-size: 28rpx;
  color: #475569;
}

.home-grid {
  margin-bottom: 32rpx;
}

.grid-card {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
  padding: 32rpx 24rpx;
  border-radius: 24rpx;
  background: #ffffff;
  box-shadow: 0 12rpx 32rpx rgba(15, 23, 42, 0.08);
  height: 100%;
}

.grid-icon {
  font-size: 44rpx;
}

.grid-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #0f172a;
}

.grid-desc {
  font-size: 26rpx;
  color: #64748b;
  flex: 1;
}

.home-card {
  margin-bottom: 32rpx;
  border-radius: 28rpx;
  background: #ffffff;
  box-shadow: 0 16rpx 40rpx rgba(15, 23, 42, 0.08);
  padding: 24rpx 32rpx 32rpx;
}

.home-progress {
  position: relative;
  margin-top: 12rpx;
}

.home-progress__loading {
  position: absolute;
  right: 16rpx;
  top: -32rpx;
  font-size: 24rpx;
  color: #64748b;
}

.home-stats {
  margin-top: 24rpx;
  display: flex;
  justify-content: space-between;
  gap: 24rpx;
}

.home-stat {
  flex: 1;
  background: #f8fafc;
  border-radius: 20rpx;
  padding: 20rpx 16rpx;
  text-align: center;
}

.home-stat__value {
  font-size: 32rpx;
  font-weight: 700;
  color: #2563eb;
}

.home-stat__label {
  margin-top: 8rpx;
  font-size: 24rpx;
  color: #64748b;
}
