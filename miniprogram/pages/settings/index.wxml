<!-- 设置页：提供语速、播音人占位选择与服务端地址配置 -->
<view class="container">
  <view class="section">
    <text class="label">播音人</text>
    <picker mode="selector" range="{{voiceOptions}}" value="{{voiceIndex}}" bindchange="onVoiceChange">
      <view class="picker-value">{{voiceOptions[voiceIndex]}}</view>
    </picker>
  </view>
  <view class="section">
    <text class="label">语速</text>
    <picker mode="selector" range="{{speedOptions}}" value="{{speedIndex}}" bindchange="onSpeedChange">
      <view class="picker-value">{{speedOptions[speedIndex]}}</view>
    </picker>
  </view>
  <view class="section">
    <text class="label">后端地址</text>
    <input class="input" value="{{baseUrl}}" bindinput="onBaseUrlInput" placeholder="例如 http://localhost:3000" />
    <button size="mini" bindtap="saveBaseUrl">保存地址</button>
  </view>
  <view class="tip">* 语速与播音人设置暂未与云端联动，后续版本将接入腾讯云 TTS。</view>
</view>
